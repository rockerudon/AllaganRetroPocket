# Libretro cores

Core binaries are not committed to this repository.

Place compatible `*_libretro.dll` files in this folder. Put required support files in `SystemFiles` and include the matching source and license notices for every core you distribute.

Do not add ROMs, protected BIOS files or proprietary firmware to this folder.
